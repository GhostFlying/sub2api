# Upstream v0.1.182 conflict resolution

## Source state

- Fork source: `fork/fork` at `4697e434ff840aa86fa98413ba5c82d869a775d3`
- Patch-stack anchor: `fork/upstream-release` at `2bc139ab527b4a687546d145dc7bb9063cf14510`
- Effective upstream baseline: `aa2c4e8d136b13553ac7bae3d76c25715333a554`
  (`v0.1.182` plus the verified VERSION-only metadata commit)

## Plan

1. Recreate `sync/upstream-v0.1.182` from the effective upstream baseline.
2. Replay `fork/upstream-release..fork/fork` in order, dropping commits that
   are empty on the new baseline.
3. Resolve non-generated conflicts by preserving both current upstream behavior
   and the fork-only feature intent. If `wire_gen.go` is the only generated
   conflict, regenerate it from the merged Wire providers.
4. Run focused tests for each conflicted area, regenerate Wire, and run the
   relevant backend and frontend validation.
5. Publish the repaired sync branch without moving `fork` or
   `upstream-release`; promotion remains a separate reviewed action.

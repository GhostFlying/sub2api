## Plan

1. Add the minimal `actions: write` permission to `Promote Fork` so it can dispatch the Docker Image workflow after promotion.
2. Validate the workflow YAML and run an Actions workflow lint check if available locally.
